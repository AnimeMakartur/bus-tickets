export class Bus {
    constructor(public id: number, public name: string, public number: string, public capacity: number) {
        this.id = id;
        this.name = name;
        this.number = number;
        this.capacity = capacity;
    }
}
